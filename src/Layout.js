import React from 'react'

const style = {
  padding: 50,
  borderRadius: 4,
  border: '1px solid #ccc',
  margin: 50,
}

const Layout = ({children}) => <div style={style}>{children}</div>

export default Layout
